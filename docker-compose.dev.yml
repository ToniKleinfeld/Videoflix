services:
  app:
    container_name: jwt_django_dev
    image: jwt_django
    ports:
      - "8000:8000"
    volumes:
      - .:/usr/src/app
    working_dir: /usr/src/app
    command: python3 manage.py runserver 0.0.0.0:8000
    stdin_open: true
    tty: true